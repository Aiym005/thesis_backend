package com.tms.thesissystem.service.notification.api;

/**
 * Removed: notifications are served by
 * {@code com.tms.thesissystem.microservices.notification.NotificationQueryController}
 * inside the notification microservice. No service component-scans this package, so this
 * controller was never registered with Spring and only added unreachable code.
 */
final class NotificationServiceController {
    private NotificationServiceController() {
    }
}
